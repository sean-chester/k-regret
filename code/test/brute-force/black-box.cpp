#include "catch2/catch.hpp"

// #include "k-regret/brute-force/bf.h"


SCENARIO( "The brute-force algorithm is black-box tested on a toy example", "[brute-force][toy][black-box]")
{
  GIVEN( "Input" )
  {
    WHEN( "Output is genreated" )
    {
     THEN( "The output is correct" )
      {
      }
    }
  }
}
